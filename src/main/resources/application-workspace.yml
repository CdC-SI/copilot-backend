workspace:
  # La liste des sources par workspace est désormais stockée en base
  # (tables workspace / source / workspace_source) et résolue via WorkspaceService.
  # Seul le workspace par défaut reste porté par la configuration.
  default-workspace: GENERAL
