spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  ai:
    vectorstore:
      pgvector:
        initialize-schema: false

II_DATA_FOLDER: ''